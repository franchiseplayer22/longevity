# Postgres connection string (Railway-hosted in prod)
DATABASE_URL="postgresql://user:password@host:5432/railway?schema=public"

# Privy passwordless auth — get from https://dashboard.privy.io
NEXT_PUBLIC_PRIVY_APP_ID=""
PRIVY_APP_SECRET=""

# Demo-mode role picker (Phase 2). Set to "1" to expose the demo card.
NEXT_PUBLIC_DEMO_MODE="0"

# Optional: enables live Tambo generative UI instead of the stub provider.
NEXT_PUBLIC_TAMBO_API_KEY=""
